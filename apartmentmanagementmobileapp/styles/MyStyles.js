import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    subject: {
        fontSize: 30,
        fontWeight: "bold",
        color: "blue",
        marginTop: 50,
    },
});
